server:
  port: 8093

spring:  
  datasource:
    url: jdbc:mysql://localhost:3308/test
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true